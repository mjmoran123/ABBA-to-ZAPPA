<!DOCTYPE html>
<html lang="en">
<head>
  <!-- 
    normalize.css removes cross-browser differences in defaults, e.g.,
    differences in how form elements appear between Firefox and IE
    See: http://necolas.github.com/normalize.css/
  -->
  <link rel="stylesheet" href="/css/normalize.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/helpers.js"></script>
  <script src="/js/application.js"></script>

  <title>ABBA to ZAPPA</title>
</head>
<body>
<header id="main-header">
  <h1>ABBA to ZAPPA</h1>
  <nav>
  <% if logged_in? %>
    <a href="/players/<%= current_user.id %>">Hi <%= current_user.username %></a>

    <select id="challenge-selector">
    <option >Choose a Challenge</option>
    <% @challenges.each do |challenge| %>
      <option href="/challenges/<%= challenge.id %>"><%= challenge.start_name + " to " + challenge.end_name %> </option>
    <% end %>
</select>
    <form class="logout-form" method="POST" action="/logout">
      <input id="logout-button" type="submit" name="Logout" value="Logout">
    </form>
  <% else %>
    <a  id="register" href="/players/new">Register</a>
    <a id="login" href="/login">Login</a>
  <% end %>
</nav>
</header>

<div class="container">
  <%= yield %>
</div>
</body>
</html>
